box: node
build:
    steps:
        - script:
            name: Install bower
            code: npm install -g bower
        - npm-install
        - bower-install
        - grunt:
            tasks: test