box: node
build:
    steps:
        - npm-install
        - script:
            name: Install bower
            code: npm install -g bower
        - script:
            name: Install packages from Bower
            code: bower install --allow-root
        - grunt:
            tasks: test